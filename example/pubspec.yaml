name: example
description: A Flutter project showing the usage of Animated Text package

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  animated_text:
    path: ../


dev_dependencies:
  #flutter_test:
  #  sdk: flutter

flutter:
  assets:
    - assets/
  uses-material-design: true